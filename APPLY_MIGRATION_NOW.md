# ✅ تطبيق الترحيل على Supabase - خطوات سهلة جداً

## 🎯 هدفك:
إضافة دعم الترجمات المتعددة اللغات للإضافات والأحجام في قاعدة البيانات

---

## 📋 الطريقة الأسهل - SQL Editor (بدون codes)

### ✅ الخطوة 1: افتح Supabase Dashboard

انسخ هذا الرابط والصقه في المتصفح:
```
https://app.supabase.com/project/ylvygdlfggcaavxexuqv
```

**ستظهر:** لوحة معلومات المشروع

---

### ✅ الخطوة 2: افتح SQL Editor

**من الشاشة:**
- ابحث في القائمة اليسرى عن كلمة: **SQL Editor**
- اضغط عليها
- ستظهر قائمة
- اضغط على الزر الملون (أحمر/برتقالي): **+ New query**

**ستظهر:** صفحة بيضاء فارغة للكتابة

---

### ✅ الخطوة 3: الصق الكود الأول

**انسخ هذا الكود بالكامل:**

```sql
-- إضافة حقول الترجمة للإضافات
ALTER TABLE menu_addons ADD COLUMN IF NOT EXISTS name_en VARCHAR(255);
ALTER TABLE menu_addons ADD COLUMN IF NOT EXISTS name_fr VARCHAR(255);
ALTER TABLE menu_addons ADD COLUMN IF NOT EXISTS name_de VARCHAR(255);
ALTER TABLE menu_addons ADD COLUMN IF NOT EXISTS name_ru VARCHAR(255);
ALTER TABLE menu_addons ADD COLUMN IF NOT EXISTS name_ja VARCHAR(255);

-- إضافة حقول الترجمة للأحجام
ALTER TABLE item_variants ADD COLUMN IF NOT EXISTS name_en VARCHAR(255);
ALTER TABLE item_variants ADD COLUMN IF NOT EXISTS name_fr VARCHAR(255);
ALTER TABLE item_variants ADD COLUMN IF NOT EXISTS name_de VARCHAR(255);
ALTER TABLE item_variants ADD COLUMN IF NOT EXISTS name_ru VARCHAR(255);
ALTER TABLE item_variants ADD COLUMN IF NOT EXISTS name_ja VARCHAR(255);

-- إضافة حقل نص رأس الإضافات
ALTER TABLE menu_items ADD COLUMN IF NOT EXISTS addons_header TEXT;
```

**في Supabase:**
- اضغط في الصفحة البيضاء
- اضغط Ctrl+A لاختيار أي كود موجود
- اضغط Ctrl+V للصق الكود الجديد

---

### ✅ الخطوة 4: اشغل الكود

**في الأعلى:**
- ابحث عن زر أسود كبير: **Run** أو **Execute**
- اضغط عليه

**ستظهر:**
- في الأسفل رسالة **خضراء** ← يعني نجح! ✅

---

## ✨ انتهى!

**الآن:**
1. اذهب لـ Dashboard: http://localhost:3000/dashboard
2. اختبر: أضف صنف جديد + إضافة جديدة
3. يجب تظهر حقول الترجمة الجديدة 🎉

---

## 🔴 إذا ظهرت رسالة خطأ

**الخطأ:** "column ... already exists"  
→ لا تقلق ✓ هذا معناه الأعمدة موجودة بالفعل

**الخطأ:** شيء آخر  
→ انسخ رسالة الخطأ وأخبرني بها

---

## 📝 ملاحظات مهمة

- ✅ الكود آمن تماماً - يضيف حقول فقط ولا يحذف أي بيانات
- ✅ الحقول الجديدة اختيارية (NULL by default)
- ✅ يمكن تشغيل الكود أكثر من مرة - آمن

---

**جرّب الآن! أخبرني إذا ظهرت رسالة خطأ. 🚀**
