# نظام الترجمة التلقائية - Digital Menu SaaS

## نظرة عامة

تم تطبيق نظام ترجمة تلقائي شامل يترجم جميع محتويات القائمة (الأصناف، الأحجام، الإضافات، الأوصاف) بناءً على اختيار اللغة من قبل المستخدم.

## المميزات الرئيسية

✅ **الترجمة التلقائية للأوصاف** - عند حفظ صنف جديد أو تعديل صنف موجود
✅ **الترجمة التلقائية للأحجام/الأنواع** - ترجمة تلقائية عند الحفظ
✅ **الترجمة التلقائية للإضافات** - ترجمة تلقائية عند الحفظ
✅ **عرض اللغة الصحيحة** - اختيار الترجمة المناسبة حسب لغة المستخدم

---

## المكونات الرئيسية

### 1. `lib/translate.js` - محرك الترجمة

**الوظيفة**: `translateText(arabicText)`

```javascript
import { translateText } from '@/lib/translate'

const translations = await translateText("قهوة سوداء")
// Returns: { ar: "قهوة سوداء", en: "Black Coffee", fr: "Café noir", de: "Schwarzer Kaffee", ru: "Черный кофе", ja: "ブラックコーヒー" }
```

**المزايا**:
- تستخدم MyMemory API (مجانية، لا تحتاج مفاتيح)
- ترجمة سريعة وموثوقة
- معالجة آمنة للأخطاء

### 2. `app/dashboard/page.js` - لوحة التحكم

#### حفظ الأصناف الجديدة (`handleAddItem`)

```javascript
// ترجمة الوصف تلقائياً
const translations = await translateText(newItem.description)

// حفظ الإضافات مع الترجمات
const addonsToInsert = await Promise.all(finalAddons.map(async (addon) => {
  const addonTranslations = await translateText(addon.name)
  return {
    menu_item_id: itemData[0].id,
    name: addon.name, // الاسم الأصلي (عربي)
    name_en: addonTranslations.en,
    name_fr: addonTranslations.fr,
    name_de: addonTranslations.de,
    name_ru: addonTranslations.ru,
    name_ja: addonTranslations.ja,
    price: parseFloat(addon.price) || 0
  }
}))

// حفظ الأحجام/الأنواع مع الترجمات
const variantsToInsert = await Promise.all(variants.map(async (variant) => {
  const variantTranslations = await translateText(variant.name)
  return {
    menu_item_id: itemData[0].id,
    name: variant.name, // الاسم الأصلي (عربي)
    name_en: variantTranslations.en,
    name_fr: variantTranslations.fr,
    name_de: variantTranslations.de,
    name_ru: variantTranslations.ru,
    name_ja: variantTranslations.ja,
    price: parseFloat(variant.price),
    is_default: variant.is_default || false
  }
}))
```

#### تعديل الأصناف الموجودة (`handleEditItem`)

نفس المنطق يتم تطبيقه على حذف وإعادة إدراج الإضافات والأحجام مع الترجمات الجديدة.

### 3. `app/menu/[id]/page.js` - عرض القائمة

#### اختيار الترجمة الصحيحة (MenuItem Component)

```javascript
// للأحجام/الأنواع
const variantName = language === 'en' && variant.name_en ? variant.name_en : 
                    language === 'ja' && variant.name_ja ? variant.name_ja : 
                    language === 'fr' && variant.name_fr ? variant.name_fr : 
                    language === 'de' && variant.name_de ? variant.name_de : 
                    language === 'ru' && variant.name_ru ? variant.name_ru : 
                    variant.name

// للإضافات
const addonName = language === 'en' && addon.name_en ? addon.name_en : 
                  language === 'ja' && addon.name_ja ? addon.name_ja : 
                  language === 'fr' && addon.name_fr ? addon.name_fr : 
                  language === 'de' && addon.name_de ? addon.name_de : 
                  language === 'ru' && addon.name_ru ? addon.name_ru : 
                  addon.name
```

---

## قاعدة البيانات (Database Schema)

### جدول `menu_items`

| الحقل | النوع | الملاحظات |
|------|-------|---------|
| `id` | UUID | المعرف الأساسي |
| `name` | VARCHAR | الاسم (بالعربية) |
| `name_en` | VARCHAR | الاسم بالإنجليزية |
| `name_ja` | VARCHAR | الاسم باليابانية |
| `description` | TEXT | الوصف (بالعربية) |
| `description_en` | TEXT | الوصف بالإنجليزية |
| `description_fr` | TEXT | الوصف بالفرنسية |
| `description_de` | TEXT | الوصف بالألمانية |
| `description_ru` | TEXT | الوصف بالروسية |
| `description_ja` | TEXT | الوصف باليابانية |

### جدول `item_variants` (الأحجام/الأنواع)

| الحقل | النوع | الملاحظات |
|------|-------|---------|
| `id` | UUID | المعرف الأساسي |
| `menu_item_id` | UUID | الصنف المرتبط |
| `name` | VARCHAR | الاسم (بالعربية) |
| **`name_en`** | VARCHAR | ✨ **جديد** - الاسم بالإنجليزية |
| **`name_fr`** | VARCHAR | ✨ **جديد** - الاسم بالفرنسية |
| **`name_de`** | VARCHAR | ✨ **جديد** - الاسم بالألمانية |
| **`name_ru`** | VARCHAR | ✨ **جديد** - الاسم بالروسية |
| **`name_ja`** | VARCHAR | ✨ **جديد** - الاسم باليابانية |
| `price` | NUMERIC | السعر |
| `is_default` | BOOLEAN | اختياري افتراضي |

### جدول `menu_addons` (الإضافات)

| الحقل | النوع | الملاحظات |
|------|-------|---------|
| `id` | UUID | المعرف الأساسي |
| `menu_item_id` | UUID | الصنف المرتبط |
| `name` | VARCHAR | الاسم (بالعربية) |
| **`name_en`** | VARCHAR | ✨ **جديد** - الاسم بالإنجليزية |
| **`name_fr`** | VARCHAR | ✨ **جديد** - الاسم بالفرنسية |
| **`name_de`** | VARCHAR | ✨ **جديد** - الاسم بالألمانية |
| **`name_ru`** | VARCHAR | ✨ **جديد** - الاسم بالروسية |
| **`name_ja`** | VARCHAR | ✨ **جديد** - الاسم باليابانية |
| `price` | NUMERIC | السعر |

---

## خطوات الترجمة الكاملة

### عند إضافة صنف جديد:

1. المستخدم يدخل الاسم والوصف والأحجام والإضافات **بالعربية**
2. عند الحفظ، يتم:
   - استدعاء `translateText(description)` للوصف
   - استدعاء `translateText(addon.name)` لكل إضافة
   - استدعاء `translateText(variant.name)` لكل حجم
3. يتم حفظ جميع الترجمات في جدول البيانات
4. في قاعدة البيانات، يتم تخزين 6 نسخ من كل حقل:
   - `name` (عربي أصلي)
   - `name_en`, `name_fr`, `name_de`, `name_ru`, `name_ja` (ترجمات تلقائية)

### عند عرض القائمة:

1. المستخدم يختار اللغة من القائمة العلوية
2. يتم تعيين المتغير `language` إلى اختيار المستخدم
3. عند عرض كل صنف:
   - يتم اختيار الحقل المناسب بناءً على اللغة
   - مثال: إذا كانت اللغة `en`، يتم عرض `name_en` بدلاً من `name`
   - إذا لم يكن الحقل موجوداً، يتم العودة إلى الاسم الأصلي (العربي)

---

## الترجمات المدعومة

| الكود | اللغة | الرمز |
|------|------|------|
| `ar` | العربية | 🇸🇦 |
| `en` | الإنجليزية | 🇬🇧 |
| `fr` | الفرنسية | 🇫🇷 |
| `de` | الألمانية | 🇩🇪 |
| `ru` | الروسية | 🇷🇺 |
| `ja` | اليابانية | 🇯🇵 |

---

## الخطوات المتبقية

### 🔴 حرج - هام جداً

**إنشاء الأعمدة الجديدة في قاعدة البيانات:**

```sql
-- إضافة أعمدة الترجمة لجدول item_variants
ALTER TABLE item_variants ADD COLUMN name_en VARCHAR(255);
ALTER TABLE item_variants ADD COLUMN name_fr VARCHAR(255);
ALTER TABLE item_variants ADD COLUMN name_de VARCHAR(255);
ALTER TABLE item_variants ADD COLUMN name_ru VARCHAR(255);
ALTER TABLE item_variants ADD COLUMN name_ja VARCHAR(255);

-- إضافة أعمدة الترجمة لجدول menu_addons
ALTER TABLE menu_addons ADD COLUMN name_en VARCHAR(255);
ALTER TABLE menu_addons ADD COLUMN name_fr VARCHAR(255);
ALTER TABLE menu_addons ADD COLUMN name_de VARCHAR(255);
ALTER TABLE menu_addons ADD COLUMN name_ru VARCHAR(255);
ALTER TABLE menu_addons ADD COLUMN name_ja VARCHAR(255);
```

**أو عبر Supabase UI:**

1. افتح [Supabase Dashboard](https://app.supabase.com)
2. اذهب إلى **SQL Editor**
3. انسخ والصق الأوامر أعلاه
4. اضغط **Execute**

---

## اختبار الميزة

### خطوات الاختبار:

1. **أضف صنف جديد** في dashboard مع:
   - اسم عربي: "فراولة"
   - وصف عربي: "فراولة طازة وحمراء"
   - إضافة: "شوكولاتة"
   - حجم: "صغير"

2. **افتح القائمة العامة** (menu page)

3. **اختر لغات مختلفة** من قائمة اللغات:
   - العربية: يجب أن ترى الأسماء الأصلية
   - الإنجليزية: يجب أن ترى الترجمات
   - اليابانية: يجب أن ترى الترجمات
   - وهكذا...

4. **انقر على الصنف** لفتح المودال:
   - تأكد من أن الأحجام مترجمة ✓
   - تأكد من أن الإضافات مترجمة ✓
   - تأكد من أن الوصف مترجم ✓

---

## استكشاف الأخطاء

### المشكلة: الأحجام/الإضافات تظهر بالعربية فقط

**الحل:**
1. تأكد من تنفيذ أوامر SQL أعلاه لإنشاء الأعمدة
2. أضف صنف جديد (يتم ترجمته تلقائياً)
3. عدّل صنف موجود ليتم ترجمته

### المشكلة: الترجمات لا تظهر بشكل صحيح

**تحقق من:**
1. هل اللغة محفوظة في `localStorage`؟
2. هل `detectLanguage()` يعمل؟
3. فتح Console وتحقق من الأخطاء

---

## ملاحظات تقنية

- **MyMemory API**: مجانية بدون قيود (2000 طلب يومي لكل IP)
- **الترجمة تحدث عند الحفظ** وليس عند العرض (أداء أفضل)
- **الأسماء الأصلية محفوظة** في حالة فشل الترجمة
- **آمن**: لا توجد مفاتيح API سرية في الكود

---

## ملفات معدلة

- ✅ `app/dashboard/page.js` - handleAddItem, handleEditItem (مع الترجمات)
- ✅ `app/menu/[id]/page.js` - MenuItem component (عرض الترجمات)
- ✅ `lib/translate.js` - محرك الترجمة (جديد)
- ⏳ قاعدة البيانات - تحتاج إلى أعمدة جديدة

---

**تاريخ آخر تحديث**: 2025
**الحالة**: جاهز للاختبار بعد إنشاء الأعمدة

