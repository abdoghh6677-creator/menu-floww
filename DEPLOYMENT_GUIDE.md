# 🚀 دليل نشر MenuFlow أونلاين مجاناً

## الخيار الأفضل: Vercel + نطاق مجاني

### الخطوة 1️⃣: إعداد GitHub (إذا لم تكن قد فعلت)

```bash
# في مجلد المشروع
git init
git add .
git commit -m "Initial commit"

# إنشاء مستودع جديد على GitHub
# 1. اذهب إلى https://github.com/new
# 2. اكتب اسم المستودع: digital-menu-saas
# 3. اختر "Private" أو "Public"
# 4. انسخ الأوامر التالية:

git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/digital-menu-saas.git
git push -u origin main
```

### الخطوة 2️⃣: ربط Vercel مع GitHub

```bash
1. اذهب إلى https://vercel.com
2. اضغط "Sign Up" → اختر "Continue with GitHub"
3. وافق على الأذونات
4. اضغط "New Project"
5. اختر المستودع "digital-menu-saas"
6. اضغط "Import"
```

### الخطوة 3️⃣: تكوين متغيرات البيئة على Vercel

```bash
في Vercel Dashboard:

1. اذهب إلى Project Settings
2. اختر "Environment Variables"
3. أضف المتغيرات:

Name: NEXT_PUBLIC_SUPABASE_URL
Value: https://xxxxx.supabase.co

Name: NEXT_PUBLIC_SUPABASE_ANON_KEY
Value: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

4. اضغط "Save"
5. ارجع إلى الـ Dashboard واضغط "Deploy"
```

### الخطوة 4️⃣: النشر التلقائي

```bash
✅ بعد كل push إلى GitHub:
- Vercel تكتشف التغييرات تلقائياً
- تبني المشروع
- تنشره مباشرة
- رابط الموقع يصبح متاحاً
```

### الخطوة 5️⃣: الحصول على دومين مجاني

**الخيار A: استخدام دومين Vercel المجاني**
```bash
1. في Vercel Dashboard → Settings → Domains
2. ستجد دومين مجاني: xxxxx.vercel.app
3. هذا الدومين يعمل تلقائياً!

مثال: menuflow-app.vercel.app
```

**الخيار B: دومين مجاني من Freenom (اختياري)**
```bash
1. اذهب إلى https://www.freenom.com
2. ابحث عن دومين (مثل: menuflow.tk)
3. اختر "Get it now"
4. استكمل عملية التسجيل المجانية
5. اذهب إلى DNS Settings
6. أضف CNAME record:
   - Name: www
   - Type: CNAME
   - Value: cname.vercel-dns.com

7. في Vercel:
   - Settings → Domains
   - أضف الدومين الجديد
   - اتبع تعليمات التحقق
```

**الخيار C: دومين مجاني من GitHub Pages + Custom Domain**
```bash
1. اذهب إلى https://namecheap.com
2. ابحث عن دومين (.tk مجاني للسنة الأولى)
3. في DNS Settings أشر إلى Vercel IP
4. في Vercel أضف الدومين المخصص
```

---

## 📋 قائمة التحقق للنشر

### قبل النشر:
- [ ] تم إنشاء مستودع GitHub خاص
- [ ] تم تحديث .env.local.example بدون بيانات حساسة
- [ ] تم حذف .env.local من المستودع
- [ ] تم اختبار الموقع محلياً: `npm run dev`
- [ ] بناء الإنتاج: `npm run build` بدون أخطاء

### أثناء النشر:
- [ ] تم ربط GitHub مع Vercel
- [ ] تم إضافة متغيرات البيئة على Vercel
- [ ] تم تفعيل الـ Deployments التلقائية
- [ ] تم التحقق من أن البناء نجح (الشاشة خضراء)

### بعد النشر:
- [ ] الموقع متاح على الرابط الجديد
- [ ] جميع الصفحات تعمل بشكل صحيح
- [ ] تسجيل الدخول والتسجيل يعمل
- [ ] قاعدة البيانات متصلة بشكل صحيح
- [ ] الصور تحمل بسرعة

---

## 🔐 أمان الإنتاج

### متغيرات البيئة الآمنة:

```bash
✅ NEXT_PUBLIC_* (آمنة للإظهار):
- NEXT_PUBLIC_SUPABASE_URL ✓
- NEXT_PUBLIC_SUPABASE_ANON_KEY ✓

❌ متغيرات حساسة (لا تستخدم NEXT_PUBLIC_):
- Service Role Key (فقط في الخادم)
- أي مفاتيح API سرية
```

### تفعيل HTTPS:

```bash
✅ Vercel يفرض HTTPS تلقائياً
✅ جميع الاتصالات مشفرة
✅ شهادة SSL مجانية موفرة من Let's Encrypt
```

### حماية من Brute Force:

```bash
الموقع محمي بـ:
- معدل الطلبات (Rate Limiting)
- التحقق من صحة المدخلات
- Supabase RLS Policies
```

---

## 🛠️ استكشاف الأخطاء

### "Build Failed" - فشل البناء:

```bash
1. افتح Vercel Logs
2. ابحث عن الخطأ
3. شيوع الأخطاء:
   - خطأ في الاستيراد: تحقق من أسماء الملفات
   - متغيرات بيئة مفقودة: أضفها في Vercel
   - مشاكل في الـ Build: جرب locally أولاً
```

### "Supabase Connection Failed" - فشل الاتصال:

```bash
1. تحقق من NEXT_PUBLIC_SUPABASE_URL
2. تحقق من NEXT_PUBLIC_SUPABASE_ANON_KEY
3. تأكد من أن Supabase مشروع يعمل
4. تحقق من CORS settings في Supabase
```

### "Domain Not Working" - الدومين لا يعمل:

```bash
1. انتظر 24-48 ساعة لانتشار DNS
2. امسح الـ Browser Cache
3. جرب في متصفح آخر
4. تحقق من DNS Records في Vercel
```

---

## 📊 المراقبة بعد النشر

### مراقبة الأداء:

```bash
في Vercel Dashboard:
- Analytics → شاهد عدد الزوار
- Performance → سرعة الصفحات
- Logs → المشاكل والأخطاء
```

### مراقبة Supabase:

```bash
في Supabase Dashboard:
- Database → حجم البيانات
- Auth → عدد المستخدمين
- Logs → أخطاء الـ queries
```

---

## 📱 الخطوات الموصى بها بعد النشر

1. **تفعيل Email Confirmation:**
   ```bash
   Supabase Dashboard → Authentication → Email Templates
   تأكد من "Confirm email" مفعّل
   ```

2. **إعداد Custom Domain:**
   ```bash
   اختر دومين احترافي (مثل menuflow.pro)
   أو استخدم دومين مجاني من Freenom
   ```

3. **إعداد Analytics:**
   ```bash
   أضف Google Analytics (مجاني)
   أو استخدم Vercel Analytics المدمج
   ```

4. **النسخ الاحتياطية:**
   ```bash
   في Supabase → Backups
   فعّل النسخ الاحتياطية التلقائية
   ```

5. **مراقبة الأمان:**
   ```bash
   راجع security_logs جدياً
   راقب محاولات تسجيل الدخول الفاشلة
   ```

---

## 💰 التكاليف (مجاني 100%)

| الخدمة | السعر | ملاحظات |
|--------|------|---------|
| **Vercel** | مجاني | النشر والـ Hosting |
| **GitHub** | مجاني | نسخة المشروع |
| **Supabase** | مجاني | 500MB قاعدة بيانات |
| **Freenom Domain** | مجاني | .tk/.ml/.ga/.cf |
| **Total** | **مجاني** | ✅ |

---

## 🎯 الرابط النهائي

بعد اكتمال جميع الخطوات:

```
https://YOUR_DOMAIN.vercel.app
أو
https://YOUR_CUSTOM_DOMAIN.com
```

**مثال:**
```
https://menuflow-app.vercel.app
أو
https://menuflow.tk
```

---

## 📞 الدعم

إذا واجهت مشاكل:

1. **Vercel Support**: https://vercel.com/support
2. **Supabase Docs**: https://supabase.com/docs
3. **Next.js Docs**: https://nextjs.org/docs

---

**التاريخ:** فبراير 2026
**آخر تحديث:** تم تطبيق أحدث الممارسات الآمنة ✅
