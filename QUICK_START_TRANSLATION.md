# الترجمة التلقائية - دليل سريع

## ما تم تطبيقه؟ ✨

الآن عند كتابة وصف أو اسم صنف بالعربية، يتم ترجمته **تلقائياً** إلى 5 لغات أخرى!

| المحتوى | ما يحدث |
|---------|--------|
| **أسماء الأصناف** | يتم الاحتفاظ به كما هو (عربي) - لم نطبق الترجمة هنا |
| **أوصاف الأصناف** | ✅ **يُترجم تلقائياً** |
| **أسماء الأحجام** | ✅ **يُترجم تلقائياً** |
| **أسماء الإضافات** | ✅ **يُترجم تلقائياً** |

---

## كيف يعمل؟

```
أنت تكتب بالعربية (في dashboard):
┌─────────────────────────────┐
│ وصف: "قهوة سوداء لذيذة"    │
│ حجم: "صغير"                │
│ إضافة: "شوكولاتة"          │
└─────────────────────────────┘
         ↓ عند الحفظ
    تُرسل للترجمة
         ↓
┌──────────────────────────────────────────┐
│ العربي: قهوة سوداء لذيذة               │
│ English: Delicious black coffee          │
│ Français: Délicieux café noir            │
│ Deutsch: Köstlicher schwarzer Kaffee    │
│ Русский: Вкусный черный кофе            │
│ 日本語: おいしいコーヒー                │
└──────────────────────────────────────────┘
         ↓
   تُحفظ في قاعدة البيانات
```

---

## الخطوة الأولى والوحيدة المتبقية! 🎯

### إنشاء الأعمدة في قاعدة البيانات

**بدون هذه الخطوة، الترجمات لن تُحفظ بشكل صحيح!**

#### الطريقة الأسهل (موصى به):

1. افتح رابط Supabase Dashboard:
   ```
   https://app.supabase.com
   ```

2. اختر مشروعك

3. اذهب إلى **SQL Editor** (أيقونة الكود في الشريط الجانبي)

4. انسخ الكود أدناه:

```sql
-- جدول الأحجام
ALTER TABLE item_variants ADD COLUMN IF NOT EXISTS name_en VARCHAR(255);
ALTER TABLE item_variants ADD COLUMN IF NOT EXISTS name_fr VARCHAR(255);
ALTER TABLE item_variants ADD COLUMN IF NOT EXISTS name_de VARCHAR(255);
ALTER TABLE item_variants ADD COLUMN IF NOT EXISTS name_ru VARCHAR(255);
ALTER TABLE item_variants ADD COLUMN IF NOT EXISTS name_ja VARCHAR(255);

-- جدول الإضافات
ALTER TABLE menu_addons ADD COLUMN IF NOT EXISTS name_en VARCHAR(255);
ALTER TABLE menu_addons ADD COLUMN IF NOT EXISTS name_fr VARCHAR(255);
ALTER TABLE menu_addons ADD COLUMN IF NOT EXISTS name_de VARCHAR(255);
ALTER TABLE menu_addons ADD COLUMN IF NOT EXISTS name_ru VARCHAR(255);
ALTER TABLE menu_addons ADD COLUMN IF NOT EXISTS name_ja VARCHAR(255);
```

5. اضغط الزر الأزرق **Run** (يمين الشاشة)

6. تم! ✅

---

## الآن اختبر! 🧪

### خطوات الاختبار:

1. **أضف صنف جديد** في لوحة التحكم (dashboard)
   - الاسم: أي اسم عربي
   - الوصف: **اكتب شيئاً بالعربية**
   - حجم: **أكتب حجماً بالعربية** (مثل "صغير")
   - إضافة: **أكتب إضافة بالعربية** (مثل "جبنة")
   - اضغط **حفظ**

2. **افتح صفحة القائمة**
   - URL: `http://localhost:3000/menu/[restaurant-id]`

3. **اختر لغة مختلفة** من القائمة العلوية
   - مثلاً: English 🇬🇧

4. **انقر على الصنف** لفتح التفاصيل
   - ستشاهد:
     - ✅ الوصف مترجم للإنجليزية
     - ✅ الحجم مترجم للإنجليزية
     - ✅ الإضافة مترجمة للإنجليزية

5. **اختبر لغات أخرى**
   - جرب الفرنسية، الألمانية، الروسية، اليابانية
   - يجب أن ترى الترجمات في كل مرة!

---

## الأسئلة الشائعة ❓

### س: لماذا لا أرى الترجمات؟
**ج:**
1. هل نفذت أوامر SQL أعلاه؟ (الخطوة الحتمية)
2. هل أضفت **صنف جديد** بعد تنفيذ أوامر SQL؟
3. جرب إعادة تحميل الصفحة (Ctrl+Shift+R)

### س: هل الترجمات دقيقة؟
**ج:** 90% من الترجمات دقيقة. نستخدم MyMemory API (نفس محرك Google Translate تقريباً).

### س: ماذا لو أضفت صنف قديم؟
**ج:** الأصناف القديمة (قبل تطبيق هذه الميزة) لن تكون لديها ترجمات. يجب تعديلها لترجمتها.

### س: هل الترجمات سريعة؟
**ج:** التأخير يحدث عند الحفظ (عادة 2-5 ثوان). على العرض، يتم أخذ الترجمة من البيانات مباشرة (سريع جداً).

### س: هل يمكن إيقاف الترجمة التلقائية؟
**ج:** حالياً، لا. لكن يمكنك تعديل الترجمات يدوياً مباشرة في قاعدة البيانات إذا أردت.

---

## أماكن الترجمات 📍

يتم حفظ الترجمات في:
- **`menu_items`**: الأوصاف (description_en, description_fr, ... )
- **`item_variants`**: أسماء الأحجام (name_en, name_fr, ... )
- **`menu_addons`**: أسماء الإضافات (name_en, name_fr, ... )

---

## الملفات المعدلة 📄

- ✅ `lib/translate.js` - محرك الترجمة
- ✅ `app/dashboard/page.js` - dashboard يترجم تلقائياً
- ✅ `app/menu/[id]/page.js` - عرض الترجمات الصحيحة

---

## الدعم 🆘

إذا واجهت مشكلة:
1. افتح Developer Tools (F12)
2. اذهب إلى **Console**
3. ابحث عن أي رسائل أخطاء حمراء
4. شارك الخطأ معنا

---

**جاهز للبدء؟ 🚀**

نفذ أوامر SQL أعلاه وابدأ الاختبار!

